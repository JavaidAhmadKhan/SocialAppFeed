import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  storyContainer: {
    marginRight: 20,
  },
  name: {
    textAlign: "center",
    fontWeight: 500,
    fontSize: 14,
    lineHeight: 14,
    marginTop: 8,
  },
  userImageContainer: {
    borderWidth: 1,
    borderColor: "#F35BAC",
    padding: 3,
    borderRadius: 50,
  },
});

export default styles;
